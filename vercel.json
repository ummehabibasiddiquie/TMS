{
  "buildCommand": "prisma generate && next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "env": {
    "DATABASE_URL": {
      "description": "MySQL database connection URL from Hostinger",
      "value": "mysql://USERNAME:PASSWORD@HOST:3306/DATABASE_NAME"
    }
  }
}
